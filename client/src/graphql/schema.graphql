type Question {
  id: ID!
  title: String
  content: String
  category: String
  level: Int
  frequency: Boolean
}

type Auth {
  accessToken: String
}

type Query {
  allQuestions: [Question!]!
  login(code: String!): Auth!
}
