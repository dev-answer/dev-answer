type Question {
  id: ID!
  title: String
  content: String
  category: String
  level: Int
  frequency: Boolean
}

type Auth {
  token: String
}

type Query {
  allQuestions: [Question!]!
  login(code: String!): Auth!
}
